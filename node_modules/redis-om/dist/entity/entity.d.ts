export declare type EntityData = Record<string, number | boolean | string | string[]>;
export declare type EntityConstructor<TEntity> = new (id: string, data?: EntityData) => TEntity;
export default abstract class Entity {
    readonly entityId: string;
    readonly entityData: EntityData;
    constructor(id: string, data?: EntityData);
}
